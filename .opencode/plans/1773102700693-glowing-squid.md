# Plan

- Update `private_dot_config/private_fish/conf.d/empty-enter.fish` only.
- Keep the existing `empty-enter` function and key bindings, but add a Fish session-scoped flag such as `__empty_enter_seen_first_input`.
- In `empty-enter`, read the current buffer with `commandline`. If the session flag is not set yet, set it immediately because the first submitted line of the session is now being consumed.
- If that first submitted line is empty, rewrite it to `ll` with `commandline -r ll`; otherwise leave the typed command untouched.
- After the first submitted line, preserve the current behavior exactly: when the buffer is empty, rewrite it to `clear`, then execute; when the buffer is non-empty, just execute it.
- Use `set -g` for the flag so it is per Fish process/session, not persistent across shells.

# Verification

- Open a fresh Fish session and press Enter on an empty prompt; it should execute `ll`.
- In the same session, press Enter again on an empty prompt; it should execute `clear`.
- Open another fresh Fish session, type a normal command first, and confirm it executes normally.
- After that normal first command, press Enter on an empty prompt and confirm it still executes `clear`.
- If vi bindings are active, verify the same behavior in `insert` and `default` maps.
